# Markdown Studio Vue

基于 Vue 3 的 Markdown 编辑器，实时预览、文档存库、深浅主题与全局字体统一。

## 功能特性

- 📝 实时 Markdown 编辑与预览，编辑区/预览区滚动联动
- 🎨 代码语法高亮（可自定义颜色），代码块与编辑区字体统一
- 🌓 深色/浅色主题切换，全局滚动条与全局字体（Start）统一
- 📁 文档管理：新建、打开、保存、删除、导入；文档存储于数据库
- 📤 多种导出格式（HTML、Markdown、PDF）
- 📊 数据库上传统计与图表（左侧栏）
- 🎚️ 可折叠左右侧边栏（左侧：数据库上传记录 + 代码高亮；右侧：我的文档 + 文档操作）
- 🔊 编辑与导出音效反馈
- 🐾 桌宠组件
- 👤 登录/注册/登出，JWT 鉴权，支持访客浏览
- 💬 社区页入口与展示

## 安装与运行

### 前置要求

- Node.js 16+
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

在浏览器打开 http://localhost:3000（注意端口占用时 Vite 会使用其他端口）。

### 构建与预览

```bash
npm run build
npm run preview
```

## 目录结构

```
frontend/
├── public/
│   └── audio/
│       ├── edit.mp3
│       ├── export.mp3
│       ├── pet.mp3
│       ├── pet.png
│       ├── start.ttf          # 全局字体
│       └── wallpaper.png
├── src/
│   ├── components/
│   │   ├── DesktopPet.vue
│   │   ├── EditorPane.vue
│   │   ├── SidebarLeft.vue
│   │   ├── SidebarRight.vue
│   │   └── TopBar.vue
│   ├── composables/
│   │   ├── useAudio.js
│   │   ├── useAuth.js
│   │   ├── useDesktopPet.js
│   │   ├── useDocument.js     # 文档列表、CRUD、统计
│   │   ├── useFileSystem.js   # 当前编辑状态
│   │   ├── useHighlightColors.js
│   │   ├── useSidebar.js
│   │   └── useTheme.js
│   ├── config/
│   │   └── env.js
│   ├── router/
│   │   └── index.js
│   ├── services/
│   │   └── api.js
│   ├── styles/
│   │   ├── main.css
│   │   └── community.css
│   ├── utils/
│   │   ├── api-tester.js
│   │   ├── audioManager.js
│   │   ├── exportUtils.js
│   │   └── markdownParser.js
│   ├── views/
│   │   ├── CommunityView.vue
│   │   ├── EditorView.vue
│   │   └── LoginView.vue
│   ├── App.vue
│   └── main.js
├── index.html
├── package.json
├── vite.config.js
├── .env.development
└── README.md
```

## 环境变量

- `.env.development`：开发环境（如 `VITE_API_BASE_URL` 指向后端 `http://localhost:8080`）
- 生产构建时可通过 `VITE_*` 变量配置 API 地址
